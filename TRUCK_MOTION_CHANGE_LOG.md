# Truck motion change log

Stable-workspace root review: removed Truck-specific workspace/sidebar sizing; only zoom controls lift above expanded timeline. Portable build passed (724ms). Measured portable Chromium before Truck / Truck / Brush at1280x800: canvas[x345,y150,w410,h410], workspace[x100,y100,w900,h510]; at600x800: canvas[x125,y206,w404,h404], workspace[x64,y156,w526,h504]. Every measurement and zoom1 identical across switches. Accepted local test, user acceptance pending; no push. Full suite not yet measured for this delta.

Latest compact-controls root review: reviewed rendered 1280x800 UI, click popup and left controls, then source correction placing opaque popup above its key. Approved local testing only, user acceptance pending. Luna reports full Chromium 68/68 (17.7s); independent root repeat 67/68 (17.4s), with existing Truck endpoint/intermediate gesture test failing; isolated rerun 1/1 (1.5s). Not consistently clean across runs. Portable rebuilt; diff check clean. No commit/push.

Latest auto-key root review: source reviewed and portable output confirmed. Complete Chromium suite **67/67 passed (24.8s)**. The 10/10 run described below was the focused Truck suite, not the complete suite. Local testing accepted; user acceptance pending. No commit or push.

Latest navigation-fix root review: accepted for local testing, user acceptance pending. Reviewed restore-before-render ordering and selected-bounds drag preservation. Exact two-ball navigation regression passes; complete Chromium suite **65/65 passed (25.3s)**. Portable rebuilt by Luna, diff check clean. No commit or push.

Latest root checkpoint: bottom-only workflow accepted for local user testing, pending user acceptance. Full Chromium suite **61/61 passed (41.9s)**. Root inspected the 1280x720 UI: no right-side Movement panel, bottom Add keyframe/Path controls present, and clicking Truck highlights Truck. Portable build was rebuilt and checked for the removed panel and new controls. No commit or push; implementation paused for user testing.

- Selected-object timeline update: only the selected object's movement row is shown; an untracked object has a blank row. Root rejected an in-progress null-row `track.id` access introduced during this update; it is now guarded. This was not established as the cause of the user's earlier Add keyframe report. Truck empty-canvas clicks clear selection and reset the label to “Select an object”. Focused Chromium Truck suite **7/7 passed (8.8s)**; the direct two-ball regression including adding independent keys, switching, removal/undo, and empty-canvas clear passed **1/1 (2.7s)**. `npm run build:portable` passed; `git diff --check` passed. No commit or push. Local user acceptance pending.

Acceptance remains pending user testing of the selected-object workflow, curved path feel, and export visuals.

Selected-object root review: null handling and deselection reviewed; portable output confirmed updated. Full Chromium run: 63/64 passed (23.5s), with the previously intermittent diamond-retiming test failing; its isolated rerun passed 1/1 (1.9s). This is not a clean full-suite pass. Two-ball selection/Add/remove/undo regression passed. Local testing only; no commit or push. Original user-reported Add failure has not been conclusively reproduced on the pre-change build.

## 2026-09-11

- Inspected the existing single-file canvas/timeline architecture and kept Truck motion scoped to foreground artwork.
- Added document-level `motionTracks` with stable IDs, per-frame tagged artwork clones, editable keyframes, straight/curved interpolation, endpoint retiming, undo/redo snapshots, frame remapping, autosave/project-file persistence, and legacy-project migration to version 6.
- Added a Truck tool button, movement inspector, end-frame/path/keyframe/preview controls, synchronized movement rows below thumbnails, and a Truck-only sampled train-track editing guide. The guide is not part of export/playback rendering.
- Protected animated artwork from silent non-Truck edits: direct transform/color/opacity/size/alignment/group/text attempts are blocked with a clear “Use Truck” notice, while Delete intentionally removes the whole animated object with a notice. Eraser now skips only tracked artwork and continues editing unrelated strokes.
- `npm run build` — passed.
- `npm test` — passed (Vitest has no unit files; `--passWithNoTests`).
- `npm run test:e2e -- --project=chromium tests/e2e/core-workflow.spec.js --grep "creates and edits a simple two-frame animation"` — passed (1/1).
- Baseline review measurements: existing Chromium smoke passed (57/57); `npm test` passed with no unit files.
- `npm run test:e2e -- --project=chromium tests/e2e/truck-motion.spec.js` — first draft passed 3/3. Review rejected draft behavior where selecting an animated object silently detached its track, erasing any frame with motion artwork, and drawing the guide from samples different from the renderer. Those behaviors were corrected before the final run.
- Review corrections: Truck selection now preserves the editable track; unrelated eraser artwork remains editable; guide rails/ties use the same clamped evaluator as rendering; the movement scroll region has a valid accessibility role; movement rows/layout are Truck-only; frame-paste remapping accepts the full pasted-frame count; pasted frame/object copies have independent motion metadata; deleting a track's start frame keeps the following artwork at that frame instead of resurrecting the previous frame.
- `npm run test:e2e -- --project=chromium tests/e2e/truck-motion.spec.js` — final clean run passed 5/5: keyframe creation/frame remapping, undo + actual save/import/reload persistence, selected-object pointer workflow with an immediate intermediate diamond assertion, multi-object/retiming/pixel export, and independent frame/object paste.
- Earlier pointer assertion exposed a layout/frame-render synchronization race; the test now waits for the selected frame's tracked clone and recomputes the live canvas box after Truck layout changes. The final rerun passed 5/5.
- Follow-up review check: axe accessibility smoke passed (1/1); root measured the complete Chromium suite at 62/62.
- `npm run build:portable` — passed (4.09s); includes Truck, `motionTracks`, and end-frame controls. `git diff --check` — passed.
- Firefox/WebKit were not verified: the baseline Firefox startup hung before assertions and was canceled; no application failure was observed.
- Follow-up review found the Movement panel was still computed as `flex` on Brush because the later generic `.prop-group` rule overrode the original hide rule. Accepted minimal correction: scope the hide rule to `.props-panel .truck-panel`; no behavior or layout refactor.
- Added a computed-style UI regression covering initial Brush hidden → Truck visible → Brush hidden → Select hidden, while the ordinary brush-size control remains visible. Focused Truck suite now passes 6/6.
- `npm run build:portable` — passed (0.70s) after the visibility fix. This is ready for local user testing; acceptance remains pending.
- Approved follow-up UI simplification: removed the right-side Movement inspector completely; the ordinary sidebar remains unchanged. Movement controls now live below the thumbnails and show only while Truck is active.
- Add keyframe now creates exactly one key at the current frame without extending the frame list. Navigating outside a track span shows a Truck-only evaluated preview; navigation alone leaves frames, history, autosave, and export unchanged. Dragging that preview creates/extends the endpoint; intermediate drags add keys.
- Added bottom Straight/Curved selection, native keyframe-diamond retiming with collision rejection and one-gesture undo, stale-track clearing when selecting another object, and playback guards for temporary previews. Text/group previews reuse the normal hit-test path.
- Replaced the old end-frame/Start movement/Preview movement UI tests with focused UI coverage: single-key → outside-span endpoint → intermediate curve, diamond retime/collision/undo, unchanged right sidebar, multiple-object save/reload, and frame/object paste independence. Focused Chromium checks: 4/4 passed; the final single-key/preview regression rerun passed 1/1.
- Portable build for this final UI delta passed (0.77s); root confirmed the removed panel and new bottom controls are present in the output. No commit or push.
- Final UI review found Truck's scrollable toolbar clipped the existing absolute brush/shape flyouts. Refused the first fixed-position-only draft after elementFromPoint confirmed the toolbar's backdrop/overflow containing context still occluded the menu.
- Accepted minimal correction: while Truck is active, an open brush/shape flyout is temporarily portaled to `document.body`, positioned beside the live toolbar viewport, and restored to its wrapper on close or tool change. The toolbar remains scrollable and ordinary non-Truck flyouts are unchanged.
- Added a 1280×720 real page-click regression for brush and shape options, including scroll-to-shape, selected-tool assertions, and elementFromPoint occlusion checks. The focused flyout check passed 1/1; motion data/history remained unchanged while opening menus.
- Root's first post-flyout full run exposed one intermittent endpoint-drag assertion in the diamond-retiming test (`[2]` instead of `[2,9]`) with the one-point preview fixture. The bounded diagnostic showed the preview center was hit-testable and the same gesture passed; the correction adds a readiness wait for the selected preview center to be in-bounds and hit-testable before dragging, with no production motion change.
- Final full Chromium suite after that correction: 62/62 passed in 22.6s. Portable build for the flyout correction passed (0.69s); `git diff --check` passed.
- Approved follow-up: added Remove keyframe beside Add keyframe. It is disabled unless the selected track has a key at the current frame, removes only that selected key, and leaves the existing track span intact so remaining endpoint positions hold rather than deleting artwork. Intermediate movement is recomputed from the remaining keys.
- Sole-key edge behavior is explicit and reversible: the existing materialized stroke has motion metadata stripped and remains ordinary artwork at its frame; the track is removed, and Undo restores the single-key track. Frame count and unrelated artwork are untouched.
- Focused Chromium remove-key regression passed 1/1, covering disabled interpolated frames, intermediate removal, endpoint removal/span preservation, sole-key bake, and undo. Acceptance remains pending user testing.
- Portable build for the Remove keyframe delta passed (0.69s).
- Strengthened the sole-key regression to compare the complete baked frame (including stroke settings/fills/holes and frame metadata after stripping only motion tags) plus the preserved frame index; renamed the test to describe removal followed by undo accurately. Focused rerun passed 1/1.
- Full Chromium suite measured 63/63 passed in 24.8s; portable build for the production delta passed in 0.69s and `git diff --check` passed.
- Follow-up navigation regression: Truck now restores the selected track before `renderUI(true)` rebuilds the movement row, so the selected row survives frame changes. Empty-click clearing remains bounded outside selected preview bounds, preserving outside-span preview dragging. Added a real two-ball regression covering second-ball creation, frame navigation, row identity/diamonds, endpoint movement, and switching back between independent tracks. Full focused Chromium Truck suite **8/8 passed (9.7s)**; portable build passed (0.69s); `git diff --check` passed. No commit or push.
- Approved lazy Truck drag: actual displacement now creates an untracked object's sole key at the current frame/drop position, while a click remains history/track-free; existing tracks save one undo snapshot per gesture and update/add the current-frame key. Added real-ball tests for click no-op, lazy drag + undo, return-to-origin, existing-key update, new-frame key creation, and independent second-track preservation. Two early full-suite retries had one intermittent pre-existing preview-drag timeout each (endpoint remained `[2]`); the isolated test passed and the final full Chromium run passed **10/10 (11.3s)**. Portable build passed (0.70s); `git diff --check` passed. No commit or push.
- Approved compact movement controls and per-key incoming interpolation: replaced the bulky Add/Remove/Path controls with previous-key, add/remove-toggle, and next-key buttons; key markers now distinguish straight diamonds from curved circles. Added an opaque body-level key menu with first-key guidance, keyboard/outside dismissal, and `aria-checked` radio states. Legacy track-level interpolation migrates onto keyframes, while mixed per-segment evaluation uses the right endpoint's mode and keeps two-key curved paths linear. Key drag retiming preserves interpolation types; save/import/undo retain them. Focused Chromium checks passed (5/5 selected motion-control and interpolation cases); full Chromium suite passed **68/68 (17.7s)**. Portable build passed **0.415s**; `git diff --check` passed. No commit or push.
- Replaced the key popup with direct key-cell behavior: clicking a non-first key toggles its incoming path straight ↔ curved, while the native title explains the mode and drag-retime behavior. The first key remains a straight diamond with no incoming path; no-key cells have no marker. Removed popup markup, CSS, listeners, and stale references. Drag retiming—including collision rejection and movement out/back to the original cell—suppresses accidental toggles and preserves interpolation. Focused direct-toggle/retiming checks passed **2/2 (4.2s)**; the requested full Chromium run measured **67/68 (47.8s)** because the known intermittent frame-4 preview-materialization wait timed out; its isolated rerun passed **1/1 (2.4s)**. Portable build passed **0.68s**; `git diff --check` passed. No commit or push.
- Removed the six Truck-only workspace/props/tools bottom and max-height overrides so activating Truck leaves the base canvas/workspace geometry, zoom, scroll, and artwork unchanged; the expanded timeline overlays the lower workspace as intended. Added real-browser geometry coverage at 1280×800 and the 600px mobile breakpoint, including returning to Brush and control visibility. Geometry regression passed **2/2 (3.3s)**; exact measured desktop canvas/workspace was `[345,150,410,410]` / `[100,100,900,510]`, and mobile was `[125,206,404,404]` / `[64,156,526,504]`, identical before/Truck/Brush. Full Chromium passed **70/70 (22.8s)**. Portable build passed **0.724s**; `git diff --check` passed. No commit or push.
- Tiny alignment polish: centered the inherited flex contents of the compact previous/add-remove/next movement controls without changing focus styling or behavior. Portable build passed **0.749s**; `git diff --check` passed. No commit or push.

## Known scope / pending acceptance

- Root review is complete for the local candidate; no commit or push was made.
- User testing remains needed for multi-object visual stacking, curved-guide appearance, and exported frame inspection.
